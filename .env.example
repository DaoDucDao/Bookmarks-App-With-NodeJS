# Copy this file to .env and adjust values for your environment.
# Keep .env out of source control; .env.example IS committed and documents
# every variable the app supports.

NODE_ENV=development
PORT=2607
DATABASE_PATH=data.db
